apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloud-native-api.fullname" . }}-config
  labels:
    {{- include "cloud-native-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}