apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-native-api.fullname" . }}
  labels:
    {{- include "cloud-native-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-native-api.selectorLabels" . | nindent 4 }}